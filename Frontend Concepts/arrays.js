// array can have hetro-geneous values (means different types of elements)

// we can not update entire array object but can update values 
// array method : push,pop,indexOf,fill ,reverse,sort,

// array is used to store the multiple values in a single variable/container which is called as Array 

const arr =[1,2,3,4]; 
console.log(arr);


// Arrays is javascript is hetrogeneous in nature : means it can store values of different types 


const arr2=["rakesh",26,'M'];
console.log(arr2);
arr2.push(true);// push is used to add element into array into back of it 
console.log(arr2);
arr2.pop() ;// this method is used to remove the last value of the array 


arr2.reverse();
console.log(arr2);


// Inbuilts function for javascript 
